# ================================================
# Supabase TEST Instance Configuration
# ================================================
#
# This file configures the ISOLATED TEST instance
# Uses different ports than development to run in parallel
#
# Setup:
#   1. Run: npm run supabase:test:start
#   2. Copy anon key and service_role key from output
#   3. Test Studio at: http://localhost:54423
#   4. Test InBucket at: http://localhost:54424
#   5. Copy this file to .env.test and fill in the keys
# ================================================

# Test instance API URL (port 54421 instead of 54321)
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54421

# Test instance anon key (get from `supabase status --config supabase/config.test.toml`)
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-test-anon-key

# Test instance service role key
SUPABASE_SERVICE_ROLE_KEY=your-test-service-role-key

# Test site URL (not actually used in tests, but required)
NEXT_PUBLIC_SITE_URL=http://localhost:4001

# ================================================
# Test Admin Configuration
# ================================================

ADMIN_EMAILS=test-admin@example.com

# ================================================
# Test Flags
# ================================================

# Enable integration test mode
INTEGRATION_TEST=true
