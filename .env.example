# ================================================
# Supabase Configuration
# ================================================
#
# FOR LOCAL DEVELOPMENT (recommended):
#   1. Run: npm run supabase:start
#   2. Copy the output values below
#   3. Studio available at: http://localhost:54323
#   4. InBucket (test emails) at: http://localhost:54324
#
# FOR CLOUD SUPABASE:
#   Get from: https://app.supabase.com/project/_/settings/api
# ================================================

# Public URL for Supabase project (safe for client-side)
# Local: http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321

# Public anonymous/publishable key (safe for client-side, used for unauthenticated requests)
# Local: Get from `supabase status` output (anon key)
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-anon-key

# Service role key (NEVER expose client-side, server-only for admin operations)
# Local: Get from `supabase status` output (service_role key)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Public site URL (for redirect operations)
# Local dev runs on port 4000
NEXT_PUBLIC_SITE_URL=http://localhost:4000

# ================================================
# Admin Configuration
# Comma-separated list of email addresses with admin access
# Case-insensitive, whitespace is trimmed
# Example: ADMIN_EMAILS=organizer@example.com,co-organizer@example.com
# ================================================

ADMIN_EMAILS=organizer@example.com

# ================================================
# Error Monitoring (Optional - Post-MVP)
# Get from: https://sentry.io/
# ================================================

SENTRY_DSN=
