-- Servus Raffle - Development Seed Data
-- This file is optional and can be used to populate dev data
-- Keep it minimal to allow for manual testing with fresh data

-- You can add development-specific data here if needed
-- For example, create a default admin user:

-- INSERT INTO auth.users (id, instance_id, aud, role, email, encrypted_password, email_confirmed_at, created_at, updated_at)
-- VALUES (
--   gen_random_uuid(),
--   '00000000-0000-0000-0000-000000000000',
--   'authenticated',
--   'authenticated',
--   'dev@example.com',
--   crypt('devpassword', gen_salt('bf')),
--   NOW(),
--   NOW(),
--   NOW()
-- );
